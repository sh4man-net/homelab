apiVersion: v1
kind: Service
metadata:
  name: homarr
  labels:
    app: homarr
spec:
  type: ClusterIP
  ports:
  - port: 7575
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: homarr
